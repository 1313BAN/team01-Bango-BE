<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.ssafyhome.domain.member.dao.MemberDAO">

    <select id="findById">
        SELECT id, email, password FROM members
        WHERE id = #{id}
    </select>

    <insert id="insertMember" useGeneratedKeys="true" keyColumn="member_seq" keyProperty="memberSeq">
        INSERT INTO members(id, password, email, roles)
        VALUES (#{id}, #{password}, #{email}, #{roles});
    </insert>
</mapper>