spring:
  application:
    name: bango

  datasource:
    url: jdbc:mysql://localhost:3306/ssafyhome_db
    password: ssafy
    username: ssafy

  #  sql:
#    init:
#      mode: always
#      schema-locations: classpath:/data.sql

  jpa:
    hibernate:
      ddl-auto: update  # ?? update
    defer-datasource-initialization: true

  data:
    redis:
      host: localhost
      port: 6379

  h2:
    console:
      path: /h2-console
      enabled: true
      settings:
        web-allow-others: true

jwt:
  secret: wKdXfChR0aAd3nOVs9R1Fg2W1MZTtGkhNQPi9z+RMxE=
