spring:
  application:
    name: bango

  datasource:
    url: jdbc:mysql://localhost:3306/ssafyhome_db
    password: ssafy
    username: ssafy

  #  sql:
#    init:
#      mode: always
#      schema-locations: classpath:/data.sql

  jpa:
    hibernate:
      ddl-auto: update  # ?? update
    defer-datasource-initialization: true

  h2:
    console:
      path: /h2-console
      enabled: true
      settings:
        web-allow-others: true
